#include <iostream>
#include <cmath>
using namespace std;

//& symbol after paramater type refers to memory location of the paramater, variable that is passed in as a parameter in main function will be the same value as the variable in the function
// void multiplication(int &a, int &b);